name: Post Release - Prepare Main for Next Development Cycle

on:
  workflow_dispatch:

env:
  AWS_DEFAULT_REGION: us-west-2

permissions:
  id-token: write
  contents: write
  pull-requests: write

jobs:
  prepare-main:
    runs-on: ubuntu-latest
    needs: check-version
    steps:
      - name: Checkout main
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 #v5.0.0
        with:
          ref: main
          fetch-depth: 0
      - name: Configure AWS credentials for BOT secrets
        uses: aws-actions/configure-aws-credentials@a03048d87541d1d9fcf2ecf528a4a65ba9bd7838 #v5.0.0
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN_SECRETS_MANAGER }}
          aws-region: ${{ env.AWS_DEFAULT_REGION }}

      - name: Get Bot secrets
        uses: aws-actions/aws-secretsmanager-get-secrets@a9a7eb4e2f2871d30dc5b892576fde60a2ecc802 #v2.0.10
        id: bot_secrets
        with:
          secret-ids: |
            BOT_TOKEN ,${{ secrets.BOT_TOKEN_SECRET_ARN }}
          parse-json-secrets: true

      - name: Setup Git
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 #v5.0.0
        with:
          fetch-depth: 0
          token: ${{ env.BOT_TOKEN_GITHUB_RW_PATOKEN }}

      - name: Configure Git
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

      - name: Get current version and calculate next minor version
        run: |
          # Get current version from Info.plist as fallback
          CURRENT_VERSION=$(grep -o '"[0-9]\+\.[0-9]\+\.[0-9]\+"' Info.plist | tr -d '"')
          echo "Current version: $CURRENT_VERSION"
          
          # Extract major, minor, patch
          MAJOR=$(echo $CURRENT_VERSION | cut -d. -f1)
          MINOR=$(echo $CURRENT_VERSION | cut -d. -f2)
          PATCH=$(echo $CURRENT_VERSION | cut -d. -f3)
          
          # Bump minor version and reset patch to 0
          NEW_MINOR=$((MINOR + 1))
          NEW_VERSION="${MAJOR}.${NEW_MINOR}.0"
          
          echo "CURRENT_VERSION=$CURRENT_VERSION" >> $GITHUB_ENV
          echo "NEW_VERSION=$NEW_VERSION" >> $GITHUB_ENV
          echo "Next version will be: $NEW_VERSION"

      - name: Create branch and update version
        run: |
          git checkout -b "prepare-main-for-next-dev-cycle-${NEW_VERSION}" main
          
          # Use the bump-version script to update version
          echo "y" | ./scripts/bump-version.sh "$NEW_VERSION"
          
          git add .
          git commit -m "Prepare main for next development cycle: Update version to $NEW_VERSION"
          git push --set-upstream origin "prepare-main-for-next-dev-cycle-${NEW_VERSION}"

      - name: Create Pull Request to main
        env:
          GITHUB_TOKEN: ${{ env.BOT_TOKEN_GITHUB_RW_PATOKEN }}
        run: |
          gh pr create --title "Post release: Update version to $NEW_VERSION" \
                       --body "This PR prepares the main branch for the next development cycle by updating the version to $NEW_VERSION in Info.plist.
          
          This PR should only be merged when the release is successful.
          
          By submitting this pull request, I confirm that you can use, modify, copy, and redistribute this contribution, under the terms of your choice." \
                       --head prepare-main-for-next-dev-cycle-${NEW_VERSION} \
                       --base main
